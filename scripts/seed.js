// Seed script runs in browser via a dedicated page or one-time init.
// Run: open the app and go to /seed (dev only) or ensure mock data is loaded when empty.
console.log(
  "AASHRAY seed: To seed data, open the app and use the Discover page (it will seed if empty) or visit /seed in development."
);
